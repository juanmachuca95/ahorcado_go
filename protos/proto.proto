syntax = "proto3";

option go_package = "./generated";

message Word {
    string word = 1;
}

message Game {
    string word = 1;
    string winner = 2;
    repeated string encontrados = 3;
}

service Ahorcado {
    rpc Ahorcado (stream Word) returns (stream Game);
}